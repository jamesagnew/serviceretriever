package net.svcret.core.ejb.ldap;

import org.junit.Test;

public class LdapAuthorizationProviderTest {

	@Test
	public void testIt() {
		
//		DefaultDirectoryService service = new DefaultDirectoryService();
//	    service.getChangeLog().setEnabled( false );
//
//	    
//	    Partition apachePartition = addPartition("apache", "dc=apache,dc=org", service.getSchemaManager(), service);
//	    
//	    LdapServer ldapService = new LdapServer();
//	    ldapService.setTransports(new TcpTransport(389));
//	    ldapService.setDirectoryService(service);
//
//	    service.startup();
//	    ldapService.start();
		
	}
	
//	 /**
//     * Add a new partition to the server
//     *
//     * @param partitionId The partition Id
//     * @param partitionDn The partition DN
//	 * @param mgr 
//	 * @param service 
//     * @return The newly added partition
//     * @throws Exception If the partition can't be added
//     */
//    private Partition addPartition( String partitionId, String partitionDn, SchemaManager mgr, DefaultDirectoryService service ) throws Exception
//    {
//        // Create a new partition named 'foo'.
//        JdbmPartition partition = new JdbmPartition(mgr);
//        partition.setId( partitionId );
//        partition.setPartitionPath( new File( partitionId ).toURI() );
//        partition.setSuffixDn( new Dn(partitionDn) );
//        service.addPartition( partition );
//
//        return partition;
//    }

}
